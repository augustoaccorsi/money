import React from 'react';
import Header from '../../components/Header';

const Transactions = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Transactions;
